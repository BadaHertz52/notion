import { isMobile } from "./mobile";
import { makeResultType } from "./result";
import { selectContent, getContent, removeSelected } from "./select";
import { setTemplateItem } from "./template";
import {
  makeNewBlock,
  findBlock,
  findParentBlock,
  getBlockText,
  findPreviousBlockInDoc,
} from "./block";
import { findPage } from "./page";
import { closeModal } from "./modal";
import { findPosition } from "./position";
export {
  isMobile,
  makeResultType,
  selectContent,
  getContent,
  removeSelected,
  setTemplateItem,
  makeNewBlock,
  findBlock,
  findParentBlock,
  getBlockText,
  findPreviousBlockInDoc,
  findPage,
  closeModal,
  findPosition,
};
